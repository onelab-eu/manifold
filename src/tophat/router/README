cf b-BGP

PEERING = establishing session

Route filtering, per neighbour filters
allow implementation of policies

filter = sequence of rules
a rule composed of two parts:
1/ MATCH logical combination of predicates = does a rule apply to a route
2/ ACTION a set of action

>>> peer X filter [in|out] 

a grammar for predicates





Scripting the interface to the router
